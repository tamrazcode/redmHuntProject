-- =================================================================
-- HUNT: Hard RP — The Corruption | Конфигурация ядра
-- =================================================================

Config = {}

-- =================================================================
-- 1. НАСТРОЙКИ RP-ЧАТА И ДИСТАНЦИЙ (В МЕТРАХ)
-- =================================================================
Config.Chat = {
    Distances = {
        Me      = 8.0,   -- /me (Обычное действие)
        Mes     = 20.0,  -- /mes, /s (Крик)
        Mew     = 3.0,   -- /mew, /w (Шепот)
        Dice    = 10.0,  -- /dice, /roll (Кости 1-10)
        OOC     = 10.0,  -- Обычный чат без команд (Локальный OOC)
    },

    Colors = {
        Me          = { 255, 170, 20 },  -- Желто-оранжевый (для /me)
        Mes         = { 220, 50, 50 },   -- Алый красный (для крика /mes)
        Mew         = { 160, 175, 190 }, -- Пепельно-серебристый (для шепота /mew)
        Dice        = { 255, 170, 20 },  -- Желто-оранжевый для костей
        DiceSuccess = { 46, 204, 113 },  -- Зеленый
        DiceFail    = { 220, 50, 50 },   -- Красный
        OOC         = { 170, 178, 189 }, -- Нейтральный серый (OOC)
        Event       = { 193, 117, 255 }, -- Фиолетовый для глобальной RP-информации
        Error       = { 235, 60, 60 },   -- Красный для ошибок
        System      = { 240, 236, 225 }, -- Костяной белый
    },
}

-- =================================================================
-- 2. НАСТРОЙКИ 3D-ТЕКСТА НАД ГОЛОВОЙ
-- =================================================================
Config.Text3D = {
    DurationMs      = 7000,
    FadeOutMs       = 1000,
    StackOffsetZ    = 0.16,
    BaseOffsetZ     = 0.45,
    TypingOffsetZ   = 0.35,
    MaxDistance     = 25.0,
    Font            = 1, -- 1 = оригинальный шрифт RDR2
    DrawBackground  = false,
    BackgroundAlpha = 180,
}

-- =================================================================
-- 3. 3 РЕЖИМА ДАЛЬНОСТИ ВОЙС-ЧАТА (ШЕПОТ, ОБЫЧНЫЙ, КРИК)
-- =================================================================
Config.Voice = {
    Modes = {
        { distance = 2.5,  label = "Шепот",   bars = 1 },
        { distance = 8.0,  label = "Обычный", bars = 2 },
        { distance = 20.0, label = "Крик",    bars = 3 },
    }
}

-- =================================================================
-- 4. ПРАВА АДМИНИСТРАТОРОВ ДЛЯ ИНСТРУМЕНТОВ
-- =================================================================
Config.AdminGroups = {
    "admin",
    "superadmin",
    "moderator"
}

-- =================================================================
-- 5. ЛОГИ ИГРОКОВ
-- =================================================================
Config.PlayerLogs = {
    File = "logs/player_activity.log",
    RetentionDays = 30,
    PageSize = 100,
}
